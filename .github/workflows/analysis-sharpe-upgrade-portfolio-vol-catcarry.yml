name: analysis-sharpe-upgrade-portfolio-vol-catcarry
on:
  push:
    branches: [analysis-kodex-409820-20260809]
    paths:
      - analysis_sharpe_upgrade_portfolio_vol_catcarry.py
      - .github/workflows/analysis-sharpe-upgrade-portfolio-vol-catcarry.yml
permissions:
  contents: write
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: analysis-kodex-409820-20260809
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install --quiet yfinance pandas numpy scipy
      - run: python analysis_sharpe_upgrade_portfolio_vol_catcarry.py > analysis_sharpe_upgrade_portfolio_vol_catcarry_result.txt
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add analysis_sharpe_upgrade_portfolio_vol_catcarry_result.txt
          git commit -m 'analysis: save portfolio vol and cat carry results' || exit 0
          git push origin HEAD:analysis-kodex-409820-20260809
