name: analysis-ratio-focus
on:
  push:
    branches: [analysis-kodex-409820-20260809]
    paths:
      - analysis_ratio_focus.py
      - .github/workflows/analysis-ratio-focus.yml
permissions:
  contents: write
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: analysis-kodex-409820-20260809
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install --quiet yfinance pandas numpy
      - run: python analysis_ratio_focus.py > analysis_ratio_focus_result.txt
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add analysis_ratio_focus_result.txt
          git commit -m 'analysis: save focused ratio robustness result' || exit 0
          git push origin HEAD:analysis-kodex-409820-20260809
