name: analysis-qdsix-attribution-longblend
on:
  push:
    branches: [analysis-kodex-409820-20260809]
    paths:
      - analysis_qdsix_attribution_longblend.py
      - .github/workflows/analysis-qdsix-attribution-longblend.yml
permissions:
  contents: write
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: analysis-kodex-409820-20260809
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install --quiet yfinance pandas numpy
      - run: python analysis_qdsix_attribution_longblend.py > analysis_qdsix_attribution_longblend_result.txt
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add analysis_qdsix_attribution_longblend_result.txt
          git commit -m 'analysis: save QDSIX attribution long blend results' || exit 0
          git push origin HEAD:analysis-kodex-409820-20260809
